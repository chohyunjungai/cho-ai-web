# REVIEW.md — 설계 검토 결과와 확정 결정 (2026-07-05)

원안 문서 3종(설계 계획서 v3, 결정사항 상세 논의, 최초 요구사항)에 대한 시니어 아키텍트 검토 결과.
**모든 판정에 대한 소유자 결정(2026-07-05): 개선 전부 채택, 누락 항목 기본값 전부 수락, 동의 12건 확정.**
이 문서는 "왜 이렇게 결정됐나"의 기록이다. 현재 유효한 명세는 [SPEC.md](SPEC.md)가 유일하다.

## 판정 요약 (16개 항목: 동의 12, 개선 4 + 인증 유보 1)

| # | 항목 | 판정 | 확정 결과 |
|---|---|---|---|
| 1 | Next.js (App Router) | 동의 | 확정 |
| 2 | Neon (PostgreSQL) | 동의 | 확정. Databricks 인수(2025) 리스크는 야간 스냅샷(락인 제거)으로 헤지 |
| 3 | Drizzle ORM | 동의 | 확정 |
| 4 | Vercel 호스팅 | 동의 | 확정 |
| 5 | 크론 실행 위치 | **개선 채택** | Vercel Cron → **GitHub Actions schedule** (동기화·야간 스냅샷). 근거: Hobby 크론 제한, 깃 커밋 작업의 홈그라운드, 실패 알림 기본 제공 |
| 6 | 짧은 링크 `/123d` 루트 숫자 슬러그 | 동의 | 확정. 302 리다이렉트(캐시 방지) |
| 7 | 클릭 기록 구현 | **개선 채택** | 미들웨어 → **루트 동적 라우트 핸들러 `app/[code]/route.ts`** + `after()` 비동기 INSERT + Neon serverless HTTP 드라이버. fail-open이 구조적으로 보장됨 |
| 8 | video_no (게시일 오름차순, 1번 시작, 결번 허용·재사용 금지, 무패딩) | 동의 | 확정 |
| 9 | 태그 체계 (업무 주축 + 기술 보조, 파셋, 통제 어휘) | 동의 | 확정. 어휘는 70개 제목 실데이터 검증 후 최종 시딩 |
| 10 | 정적 우선 ISR + fail-open | 동의 | 확정 |
| 11 | DB 스키마 | **개선 채택** | ① `videos.status` 추가(삭제 대신 상태 변경 — ID 불변) ② `clicks.user_agent` 원문 저장, 봇은 조회 시 필터 ③ 서점 URL을 `book_links` 테이블로 정규화 ④ 인덱스 명시 |
| 12 | 책 링크 `/out/` 경유 | 동의 | 확정 |
| 13 | 템플릿 모노레포 `cho-ai-templates` | 동의 | 확정 |
| 14 | 스냅샷 깃 커밋 | **개선 채택** | **별도 private 저장소 `cho-ai-data`** 신설. 클릭 로그(referrer·UA)는 공개 저장소 금지, 코드 히스토리 오염 방지 |
| 15 | 인증 라이브러리 | **개선 채택** | 요구사항(구글 로그인 + RLS 확장 가능)만 고정, 라이브러리는 [미정] — 3단계 착수 시점 생태계로 결정 |
| 16 | 로드맵 순서 | 동의 | 확정. 단, 2단계 설명란 일괄 수정에 YouTube OAuth(쓰기) 필요함을 선행 조건으로 명시 |

## 누락 결정 A~H (기본값 전부 수락)

| # | 항목 | 확정값 |
|---|---|---|
| A | 사이트 코드 저장소 | **공개, `cho-ai-web`, 개인(비학교) 계정** |
| B | 스냅샷 저장소 | **private `cho-ai-data`** |
| C | 클릭 대시보드 보호 | 방식은 [미정] — 2단계 착수 시 확정 (환경변수 토큰 또는 Vercel 보호 중) |
| D | YouTube API 준비물 | 0단계 = 읽기용 API 키만. 쓰기 OAuth는 2단계 선행 조건 |
| E | 슬러그 정규화 | **소문자로 정규화 후 매칭** (`/123D` → `/123d`) |
| F | www 처리 | **www → apex 301** (Vercel 설정) |
| G | 공개 `/api` | 캐시 헤더 우선, rate limit은 문제 발생 시 |
| H | 백업 복원 리허설 | **0단계 완료 기준에 포함** (스냅샷 → 새 Neon 브랜치 복원 1회 성공) |

## 이월된 미결 항목

Shorts 포함 여부, 태그 실데이터 검증, 서점 제휴 가입, 템플릿 정리 시트 위치, GA4 병행 여부, 대시보드 보호 방식, 인증 라이브러리 → [OPEN-QUESTIONS.md](OPEN-QUESTIONS.md)
