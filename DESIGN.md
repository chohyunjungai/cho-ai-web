# DESIGN.md — cho-ai.com 디자인 명세

확정일: 2026-07-05 · 상위 문서: [SPEC.md](SPEC.md) · 마스코트 원본: `슉슉이10개.png`

## 0. 디자인 헌법 (모든 화면 결정의 기준)

> **"유튜브에서 온 사용자가 새로 배울 것이 없는 사이트."**
> cho-ai.com은 유튜브 화면과 거의 흡사하게 만든다 — 배경색, 폰트 크기·종류, 아이콘, 카드, 칩, 간격 전부. 유일한 차이는 ① 포인트 컬러가 레드가 아니라 그린, ② 슉슉이가 상태·안내를 전달한다는 것.

**정보 구조는 무료 템플릿 스토어다** (참조 모델: thegoodocs.com). 이 사이트의 주 기능은 쇼핑몰과 같다 — 상품 = 템플릿·강의안, 상품 이미지 = 실제 시트/문서 미리보기, 매대 = 템플릿 갤러리, 구매 버튼 = [사본 만들기] (무료·즉시). 영상은 상품의 데모 영상 역할이다. **시각 언어는 유튜브, 정보 구조는 스토어.**

디자인 결정이 갈릴 때의 판정 순서:
1. 유튜브는 이걸 어떻게 하는가? → 그대로 따른다 (색만 그린 계열로 치환)
2. 정보 구조·동선이 갈리면 → 스토어라면 어떻게 하는가 (thegoodocs·쇼핑몰 문법)
3. 유튜브에 없는 요소(사본 버튼, 권한 안내 등) → 유튜브의 기존 컴포넌트 문법(필 버튼, 바텀시트, 칩)으로 조립한다
4. 그래도 모호하면 → "페이지당 핵심 행동 1개"에 유리한 쪽

**무안내 원칙**: 유튜브처럼 설명 없이 행동할 수 있어야 한다. "사본 만들기란…" 류의 상시 안내 문구를 두지 않는다 — 버튼·이미지·배치가 스스로 설명한다. 유일한 예외는 requires_auth 템플릿의 권한 예고 바텀시트(§5-3, 1회).

### UX 확정 결정 (2026-07-05)

| 결정 | 확정 | 근거 |
|---|---|---|
| 사이트 정보 구조 | **템플릿 스토어 중심** — 홈 = 템플릿 매대, 영상 갤러리는 보조 | 주 기능은 유용한 템플릿을 찾아 사본 만들기. thegoodocs 스토어 패턴 채용 |
| 영상 상세 첫 화면 | 컨텍스트 라인 → **프로모 배너(책·강의)** → 템플릿 카드. **폴드(스크롤 0) 안에 프로모와 [사본 만들기] 1개가 함께 보여야 한다** | 방문 목적(사본)을 폴드 밖으로 밀지 않으면서 홍보 노출 극대화 |
| 영상 상세의 임베드 | **제거** — 컨텍스트 라인 썸네일 탭 시 유튜브 새 탭 | 방문자는 방금 그 영상을 보고 왔다. 큰 임베드는 첫 화면 낭비 + 유튜브로 보내는 게 채널에도 이득 |
| 상시 안내 문구 | **제거** (무안내 원칙) | 유튜브처럼 설명 없이 동작. 예외는 §5-3 권한 예고 1회뿐 |
| 컨텍스트 문구 분기 | 짧은 링크 302가 `?via={slug}`를 붙임 → 클라이언트에서 문구 분기. 갤러리 유입(쿼리 없음)은 다른 문구 | 정적 서빙 유지하면서 유입 경로별 문구 가능 |
| 갤러리 목록 | **무한 스크롤** | 유튜브와 동일한 탐색 감각. 라이선스·소유자 정보는 푸터 대신 상단 about 링크와 상세 페이지에 배치해 도달 문제 해소 |
| 다크 모드 | **지원, 기본은 시스템 설정** | 유튜브가 지원하므로 미러. 토큰을 시맨틱으로 설계해 두 테마가 같은 코드 경로 |
| 홈·갤러리 진입점 | **태그 칩 행** (그룹 카드 없음) | 유튜브 홈 상단 칩과 동일 패턴 — 4개 사용자층이 각자 자기 단어(유튜브 운영/성적·평가/AI 학습…)를 칩에서 즉시 발견 |

## 1. 디자인 토큰

### 1-1. 컬러 — 유튜브 팔레트에서 레드만 그린으로 치환

원칙: 무채색 축(배경·텍스트·보더·**버튼**)은 유튜브 실측값을 그대로 쓴다. **버튼은 유튜브의 연회색 필(pill)이다 — 채움 그린 버튼은 만들지 않는다.** 그린은 유튜브가 레드를 쓰는 자리(로고·알림 뱃지·진행 표시)와 텍스트 링크에만. 구글 공식 그린(#34A853, #188038)과 겹치지 않는 톤.

```css
:root {
  /* 무채색 — 유튜브 라이트 */
  --bg:              #FFFFFF;   /* 페이지 배경 */
  --bg-raised:       #FFFFFF;   /* 카드 (라이트에선 배경과 동일, 보더로 구분) */
  --text:            #0F0F0F;   /* 본문·제목 */
  --text-secondary:  #606060;   /* 날짜·메타 */
  --border:          rgba(0,0,0,.10);

  /* 인터랙션 회색 — 유튜브 그대로 ("+만들기" 필 버튼과 동일) */
  --btn-bg:          #F2F2F2;   /* 버튼·칩·검색창 기본 */
  --btn-bg-hover:    #E5E5E5;   /* 호버 시 살짝 진하게 */
  --btn-text:        #0F0F0F;
  --hover:           #F2F2F2;   /* 흰 배경 위 메뉴·리스트·카드 호버 (둥근 모서리) */
  --selected:        #E5E5E5;   /* 선택된 메뉴·리스트 항목 (호버보다 진하게 유지) */

  /* 그린 — 유튜브의 레드 자리 (버튼 아님) */
  --brand:           #2BA05F;   /* 로고·뱃지·진행 표시 (슉슉이 몸통 톤과 동족) */
  --link:            #0F7A3C;   /* 텍스트 링크·강조 라벨 (흰 배경 대비 5.4:1 ✓) */

  /* 상태 */
  --success:         #0F7A3C;
  --warning:         #B45309;
  --error:           #CC0000;   /* 오류만 레드 — 신호색으로서의 레드 */
  --focus-ring:      #0F7A3C;
}

[data-theme="dark"] {
  /* 무채색 — 유튜브 다크 */
  --bg:              #0F0F0F;
  --bg-raised:       #212121;   /* 바텀시트·메뉴 */
  --text:            #F1F1F1;
  --text-secondary:  #AAAAAA;
  --border:          rgba(255,255,255,.20);

  --btn-bg:          #272727;
  --btn-bg-hover:    #3F3F3F;
  --btn-text:        #F1F1F1;
  --hover:           #272727;
  --selected:        #3F3F3F;

  --brand:           #3BC46D;
  --link:            #3BC46D;   /* 다크 배경 대비 8.5:1 ✓ */

  --success:         #3BC46D;
  --warning:         #F59E0B;
  --error:           #FF6B6B;
  --focus-ring:      #3BC46D;
}
```

- 테마 전환: `prefers-color-scheme` 기본 + 헤더 메뉴에 수동 토글(선택값은 localStorage) — 유튜브와 동일한 모델.
- 유튜브 썸네일(레드 포함 원색)과의 조화: UI 무채색 + 그린 포인트 소량이라 썸네일이 어떤 색이어도 충돌 없음 — 유튜브가 매일 증명하는 조합.

### 1-2. 타이포그래피 — 유튜브 스케일 그대로

```css
:root {
  --font: "Roboto", "Noto Sans KR", -apple-system, "Apple SD Gothic Neo",
          "Malgun Gothic", sans-serif;   /* 유튜브와 동일 계열 */

  /* 유튜브 실측 스케일 (모바일 기준) */
  --fs-title-lg: 18px;  --lh-title-lg: 24px;  /* 페이지 제목, 상세 제목 */
  --fs-title:    16px;  --lh-title:    22px;  /* 카드 제목(데스크톱), 섹션 헤더 */
  --fs-body:     14px;  --lh-body:     20px;  /* 카드 제목(모바일), 본문, 칩, 버튼 */
  --fs-caption:  12px;  --lh-caption:  16px;  /* 날짜, 메타, 뱃지 */

  --fw-regular: 400;  --fw-medium: 500;  --fw-bold: 700;
}
```

- 카드 제목 = 14px/500 두 줄 말줄임, 메타 = 12px/400 `--text-secondary` — 유튜브 홈과 동일.
- 블로그 본문만 예외적으로 16px/28px (긴 글 가독, 유튜브에는 없는 콘텐츠 유형).
- 웹폰트: Roboto(라틴)는 시스템 수준 보급 + Noto Sans KR은 `font-display: swap`, 서브셋(KS X 1001) woff2. 폰트 로딩 전에도 Apple SD Gothic Neo/Malgun으로 즉시 렌더 — 인앱 브라우저에서 빈 텍스트 금지.

### 1-3. 간격·라운딩·그림자·아이콘

```css
:root {
  --sp-1: 4px; --sp-2: 8px; --sp-3: 12px; --sp-4: 16px; --sp-6: 24px; --sp-8: 32px;

  --r-thumb: 12px;   /* 썸네일·카드 이미지 — 유튜브 썸네일 라운딩 */
  --r-chip:  8px;    /* 칩 — 유튜브 칩 라운딩 */
  --r-pill:  20px;   /* 버튼(높이 40px의 절반) — 유튜브 필 버튼 */
  --r-sheet: 16px 16px 0 0;  /* 바텀시트 상단 */

  --shadow-sheet: 0 -2px 16px rgba(0,0,0,.2);  /* 바텀시트만. 카드에는 그림자 없음 — 유튜브 카드는 플랫 */
}
```

- 아이콘: Material Symbols Outlined (유튜브와 같은 구글 아이콘 언어), 24px, `currentColor`. 자체 제작 아이콘 금지.
- 페이지 좌우 패딩 모바일 16px, 카드 사이 세로 간격 24px — 유튜브 홈 리듬.

## 2. 슉슉이 사용 지침

### 2-1. 에셋 규격

- **배경 투명이 절대 규칙**: 원본에서 캐릭터(+발밑 손그림 그림자)만 추출한 투명 PNG/WebP. 페이지 배경(`--bg`)이 곧 슉슉이의 배경 — 라이트·다크 어디든 그대로 앉는다.
- 포즈별 개별 파일: `shukshuk/{pose}.webp` (알파 포함), 표시 크기의 2배 해상도, 파일당 30KB 이하 목표. SVG 벡터화는 하지 않는다 — 손그림 잉크 라인의 질감이 벡터화에서 죽는다. [미정] 추후 외곽선 품질 이슈 시 재검토.
- 다크 모드 대응: 잉크 블랙 외곽선이 다크 배경에서 묻히므로, 다크에서만 `filter: drop-shadow(0 0 1px rgba(255,255,255,.35))`로 은은한 라이트 아웃라인 보정.

### 2-2. 포즈 매핑 (원본 10종)

| # | 포즈 | 쓰는 곳 |
|---|---|---|
| 1 | 손 흔들며 인사 | /about 인사 (홈에는 등장하지 않음 — 홈은 매대) |
| 2 | 전구·반짝 아이디어 | 사본 만들기 안내 시트, 블로그 팁 콜아웃 |
| 3 | 팔 X자 | 404, 존재하지 않는 짧은 링크 |
| 4 | 돋보기+시트 | 검색 결과 상단 (결과 있음) |
| 5 | 책 읽기 | 블로그 목록 헤더, 가이드 문서 |
| 6 | 망토 질주 | 로딩·처리 중 (드물게 — 정적 서빙이라 거의 안 씀) |
| 7 | 턱 괴고 고민 | 검색 0건, 템플릿 없는 영상의 빈 상태 |
| 8 | 땀 닦기 (Phew) | 오류 후 복구 안내, 일시적 실패 |
| 9 | 엄지 둘 | 사본 링크 클릭 직후 확인 토스트 |
| 10 | 콘페티 점프 | 축하 (첫 방문 환영 아님 — 마일스톤·이벤트 전용) |

### 2-3. 희소성 원칙 (도배 금지)

1. **화면당 슉슉이 최대 1회.** 헤더 로고 속 슉슉이는 예외(브랜드 마크로 상시).
2. 슉슉이는 **상태와 안내의 전달자로만** 등장 — 콘텐츠 영역(썸네일 그리드, 템플릿 카드 목록) 안에는 절대 넣지 않는다. 썸네일과 경쟁하는 순간 소음이 된다.
3. 정상 흐름(갤러리 탐색, 상세 열람)에는 등장하지 않는다. 빈 상태·오류·안내·축하처럼 "말을 걸어야 하는 순간"에만.
4. 크기: 빈 상태·안내 시트 96~128px, 토스트·콜아웃 48px. 히어로에서도 160px 초과 금지.

### 2-4. 구글 공식 오인 방지

- 헤더 로고 = 슉슉이 마크 + **"조현정의AI실험실"** 워드마크 (띄어쓰기 없음, 채널명 그대로 — 유튜브에서 온 사용자가 같은 이름을 보고 안심). 구글·YouTube 로고타입 모방 금지.
- **로고 아이콘은 테마 불변**: 라이트/다크 모두 동일 색 — 몸통 그린 #2BA05F + 셀 흰색 + 잉크 눈. 다크에서 셀을 검게 뒤집지 않는다.
- "Google과 무관한 개인 사이트" 명시는 **/about에서만** — 푸터에 반복하지 않는다 (푸터는 표준 관례대로 저작권·링크·문의만).
- 그린 사용은 --brand/--primary 두 값만 — 구글 4색 조합(파랑·빨강·노랑·초록 병치)을 절대 만들지 않는다.

## 3. 핵심 컴포넌트

### 3-1. 영상 카드 (갤러리·홈)

- 구조: 썸네일 16:9 (`--r-thumb`, i.ytimg.com, lazy) → 제목 14px/500 2줄 말줄임 → 메타 12px (`#123 · 2026.05.12`) → 템플릿 뱃지.
- **템플릿 뱃지**: 썸네일 우하단 `📋 2` (유튜브의 영상 길이 뱃지와 같은 위치·스타일, 배경 rgba(0,0,0,.8) 흰 글자). 템플릿 0개면 뱃지 없음. 이 뱃지가 이 사이트 카드의 유일한 고유 요소다.
- 모바일: 1열 풀블리드에 가까운 큰 카드(유튜브 홈과 동일), 세로 스크롤. 데스크톱: §4-0 밀도 표 (3~4열), 간격 16px.
- 카드 전체가 탭 영역 (44px 규칙 자동 충족).

### 3-2. 템플릿 카드 — 이 스토어의 상품 카드

**갤러리형 (홈·/templates — 매대)**: thegoodocs 상품 카드 문법.
- **미리보기 이미지가 주인공**: 실제 시트/문서 스크린샷 4:3 (`--r-thumb` 12px, lazy). 아이콘이 아니라 "물건 사진" — 스토어의 신뢰는 상품 이미지에서 나온다. 이미지는 `templates.preview_path`.
- 이미지 → 제목 14px/500 2줄 → 유형 뱃지(시트/문서, 이미지 좌하단 — 영상 카드의 📋 뱃지와 같은 스타일) + 연결 영상 번호(#123, 12px secondary). **카드 안에 버튼 없음.**
- 카드 탭 → 상세 페이지. **매대는 탐색, 전환은 상세에서** (thegoodocs 패턴) — 어떤 시트인지 확인하고, 사용법을 보고, 그다음 [사본 만들기]. 매대 카드에 버튼이 없어야 카드가 조용해지고 미리보기 이미지가 말한다.
- 예외: **영상 상세의 행형 카드에는 [사본 만들기]가 있다** — 짧은 링크로 온 방문자는 이미 영상에서 시트를 봤으므로 확인 단계가 끝난 상태다.
- 열 수는 §4-0 밀도 표를 따른다 (모바일 2열 → 데스크톱 4~5열).

**행(row)형 (영상 상세 안)**: 좌측 미리보기 썸네일 96×72 → 제목 + 한 줄 설명 → [사본 만들기] 전폭. 갤러리형보다 납작하게 — 폴드 안에 1개 이상 들어가야 하므로.

- `requires_auth=true`면 제목 아래 캡션: `⚡ 자동화 스크립트 포함` (12px secondary — 겁주지 않는다, 상세 설명은 §5-3 시트가 담당).
- 보더 1px `--border`, 라운딩 12px, 그림자 없음.

### 3-2b. 프로모 배너 — 책·강의 (영상 상세 상단)

목적: 무료 자료를 받으러 온 방문자에게 운영자의 유료 상품(책, 강의)을 **자연스럽게** 노출. 광고처럼 보이는 순간 실패다.

- **구조 (서점 스타일, 2026-07-06 확정)**: 카드 배경(`--bg` + 보더) 위에 **표지 원본 이미지 + 그림자** (box-shadow, 라운딩 3px — 서점처럼 흰 바탕에 책이 떠 있는 연출. 색 박스에 가두지 않는다) → 우측에 4줄: **제목(15px/700) · 부제(12px secondary) · 카피 1줄(13px) · 저자 표기(12px secondary)** → **서점 버튼 3개 [교보문고][예스24][알라딘]** (32px 소형 필, 각각 `/out/{store}/{isbn}` 경유 302 — outbound_clicks 기록). 중간 단계 없이 바로 서점으로.
- **홍보 멘트·눈썹 없음** — "이 채널을 만든 사람의 책" 류 폐기. **저자 표기 "조현정 저 | 골든래빗"이 그 역할을 대신한다** — 사이트명(조현정의AI실험실)과 저자명이 같아 '채널 주인의 책'임이 표기만으로 전달된다.
- **노출 위치 정책 (2026-07-06 확정)**:
  1. **영상 상세** (짧은 링크 도착 포함): 모바일 = 컨텍스트 라인 아래 폴드 내 / 데스크톱 = 우측 레일 최상단. video_books 연결 책 우선, 없으면 기본 프로모(promos).
  2. **홈**: 첫 상품 행(템플릿 6개) **바로 아래**, 최신 영상 행 위 — "템플릿이 먼저, 그러나 금방 책을 만난다".
  2b. **태그 페이지(/tags/*)**: 템플릿 그리드 아래·영상 섹션 위 — 칩 필터로 들어온 탐색자에게도 같은 자리에서 노출 (2026-07-06 추가).
  3. **템플릿 상세에는 넣지 않는다** — 전환([사본 만들기]) 직전 페이지에 다른 CTA를 두지 않는다 (하지 말 것 1과 동일 원리).
  4. 페이지당 배너는 1개, 홈·상세 공통으로 같은 컴포넌트·같은 스타일.
  5. **표지 크기는 반응형** (2026-07-06): 모바일 88px / 데스크톱 가로 배너 170px + 텍스트 비례 확대 / 데스크톱 우측 레일 190px — 데스크톱에서 시트·영상 카드 대비 책이 왜소해 보이지 않게.
- 배경: `--btn-bg`(연회색) 라운딩 16px — 콘텐츠와 구분되되 배너 광고 색(원색) 금지.
- **데이터 소스 우선순위**: ① `video_books`에 연결된 책 → ② 없으면 활성 `promos` 중 우선순위 1위(책 또는 강의). 배너는 **항상 1개** — 로테이션·캐러셀 금지.
- 탭 동작: 인터랙티브 요소는 서점 버튼 3개뿐 (강의 프로모일 땐 [강의 보기] 버튼 1개, 외부 링크 새 탭). 표지·카피 영역은 탭 대상 아님 — 버튼이 명확하면 배너 전체 탭은 불필요.
- 위치 근거: 폴드 안 최상단이지만 높이를 제한해 [사본 만들기]가 반드시 폴드 안에 남는다 — 홍보가 목적 행동을 밀어내면 이탈로 둘 다 잃는다.

### 3-2c. 컨텍스트 라인 (영상 상세 최상단)

- 한 줄 높이(~56px): 작은 썸네일 72×40 + 라벨 12px `--link` + 영상 제목 1줄 말줄임.
- **문구 분기** (짧은 링크 302가 붙이는 `?via` 쿼리로 판별, JS 분기 — 페이지는 동일 정적 HTML):
  - `?via` 있음 (유튜브에서 옴): `지금 보신 영상의 자료예요`
  - 없음 (사이트 내 이동): `이 영상에서 소개한 자료예요`
- 썸네일/제목 탭 → 유튜브 영상 새 탭 (임베드 대체 — 다시 보고 싶은 사람은 유튜브로).

### 3-3. 버튼 — 유튜브 연회색 필, 높이 전 사이트 통일

- **모든 버튼은 같은 규격**: 높이 40px, `--btn-bg` 배경 + `--btn-text` 글자 14px/500, 완전 필(`border-radius: 20px`), 그림자·보더 없음 — 유튜브 "+만들기" 버튼과 동일한 색·도형. 높이가 다른 버튼 변종을 만들지 않는다.
- 호버: `--btn-bg-hover`로 살짝 진해짐(100ms). pressed: scale .98. 이 변화 자체가 "누르면 반응한다"는 신호다.
- [사본 만들기]의 시각 위계 1위는 색이 아니라 **전폭(full-width) + 스크롤 0 배치 + 📋 아이콘**으로 만든다. 서점·깃허브 등 보조 버튼은 내용 폭 필로 구분.
- 상태: 클릭 시 새 탭으로 구글 /copy 열림 + 슉슉이 ⑨ 엄지 토스트 "새 탭에서 구글 사본 화면이 열렸어요".
- `requires_auth=true`인 템플릿은 클릭 시 **바로 이동하지 않고 안내 바텀시트 선노출** (§5-3). 일반 템플릿은 즉시 이동 — 마찰 추가 금지.

### 3-3b. 호버·선택 상태 — 클릭 가능성의 신호 (전 컴포넌트 공통)

유튜브의 문법 그대로:
1. **흰(검) 배경 위 인터랙티브 요소**(메뉴 항목, 리스트 행, 아이콘 버튼, 카드): 호버 시 `--hover` 배경이 **둥근 모서리(8~16px)**로 나타난다. 배경 변화 없는 클릭 요소는 존재하면 안 된다.
2. **회색 필 요소**(버튼, 칩): 호버 시 `--btn-bg-hover`로 한 단계 진해진다.
3. **선택 상태**: 선택된 메뉴·필터 항목은 `--selected` 배경이 유지된다(호버보다 진함). 선택 칩만 예외 — 유튜브처럼 반전(라이트: 검정 배경+흰 글자).
4. 영상 카드 호버(데스크톱): 카드 전체에 `--hover` 배경 + 16px 라운딩 (유튜브 홈 카드 호버와 동일).
5. 터치 기기에서는 호버 대신 pressed 상태(동일 색)로 대체.

### 3-4. 태그 칩 행

- 유튜브 홈 칩 그대로: 높이 32px, 좌우 패딩 12px, `--btn-bg`(호버 시 `--btn-bg-hover`), 8px 라운딩, 14px/500, 가로 스크롤(스크롤바 숨김), 선택 시 라이트=`#0F0F0F` 배경+흰 글자 / 다크=`#F1F1F1` 배경+검은 글자 (유튜브의 선택 칩과 동일 — 그린 아님).
- 순서: [전체] 고정 첫 자리 → 업무 태그(콘텐츠 수 내림차순) → 끝에 [태그 전체보기]. 기술 태그는 칩 행에 넣지 않고 검색·상세·/tags에서만.
- 4개 사용자층이 첫 화면 칩에서 각자 자기 단어를 발견하는 것이 이 컴포넌트의 존재 이유 — 시딩 후 콘텐츠 수 기준 정렬로 자연 달성, 수동 큐레이션 금지.

### 3-5. 검색

- 유튜브 모바일 패턴: 헤더 돋보기 아이콘 → 탭하면 전면 검색 화면(입력 자동 포커스, 최근 검색어). 데스크톱: 중앙 필 검색창.
- 결과 화면: 영상·템플릿 혼합 리스트 + 상단 필터 칩(전체/영상/템플릿/글).

### 3-6. 무한 스크롤

- 24개 단위 로드, 하단 400px 전 프리페치, 로딩 표시는 스켈레톤 카드 3개(스피너 아님 — 유튜브 방식).
- 스크롤 위치 복원 필수 (뒤로가기 시 — 인앱 브라우저 포함 검증).
- 라이선스·about·채널 링크는 푸터 의존 금지 → 헤더 메뉴와 /about, 상세 페이지 하단에 배치. 푸터 자체는 존재하되(§4-1) 거기에만 있는 정보를 만들지 않는다.

### 4-1. 공통 푸터 (2026-07-06 확정 — 대형 사이트 관례)

전 페이지 공통 2행, 과장·설명 없이:
1. **링크 행**: 소개 · 유튜브 채널 ↗ · GitHub ↗ · 문의 creator@cho-ai.com (mailto)
2. **저작권 행** (12px secondary): `© 2026 조현정의AI실험실 · 코드 MIT · 콘텐츠 CC BY 4.0 (출처 표기 후 자유 이용)`
- "Google과 무관" 류 면책 문구는 푸터에 넣지 않는다 — /about이 담당. 푸터는 짧을수록 신뢰다.

## 4. 페이지 레이아웃

모바일이 기준(주 유입이 유튜브 앱 인앱 브라우저), 데스크톱은 **축소가 아니라 재배치** — 유튜브가 폰과 데스크톱에서 다른 레이아웃을 쓰듯, 데스크톱에선 한 화면에 더 많이 보이게 한다.

### 4-0. 데스크톱 확장 규칙 (≥1024px)

**밀도 (열 수) — 유튜브처럼 유동(fluid) 그리드, 고정 컨테이너 없음:**

| 뷰포트 | 템플릿 카드 | 영상 카드 |
|---|---|---|
| < 600px (모바일) | 2열 | 1열 |
| 600~1023px (태블릿) | 3열 | 2열 |
| 1024~1439px | 4열 | 3열 |
| ≥ 1440px | 5열 | 4열 |

- **미니 가이드 레일**: 데스크톱에선 좌측 72px 아이콘 레일 고정 (유튜브 미니 가이드와 동일) — 홈 / 템플릿 / 영상 / 블로그 / 소개. 모바일에선 없음(헤더로 충분).
- **검색창**: 데스크톱은 헤더 중앙 필 검색창 상시 노출 (모바일은 아이콘 → 전면 검색).
- 카드 제목은 데스크톱에서 16px (유튜브 데스크톱과 동일), 모바일 14px.
- 호버 상태(§3-3b)는 데스크톱의 밀도를 감당하는 장치 — 카드가 많아져도 호버가 "지금 이것"을 짚어준다.

**페이지별 데스크톱 재배치:**

- **영상 상세 = 2컬럼** (유튜브 시청 페이지 미러): 좌측 주 컬럼(~2/3) = 컨텍스트 라인 → 템플릿 행형 카드들([사본 만들기]). **우측 레일(~1/3, 유튜브의 '관련 영상' 자리) = 최상단에 프로모 카드(책 표지 세로 대형) + 그 아래 "다른 자료 둘러보기" 카드 리스트.** 유튜브에서 시선이 가장 자주 가는 보조 영역이 곧 홍보·회유 자리가 된다. 첫 화면에 사본 버튼·프로모·둘러보기가 전부 보인다 — 모바일의 폴드 계약이 데스크톱에선 저절로 충족.
- **템플릿 상세 = 2컬럼** (쇼핑몰 상품 상세 문법): 좌측 = 미리보기 이미지 크게(탭 시 확대), 우측 = 제목 + 유형·태그 + [사본 만들기] + 버전·깃허브. 사용법 body_md는 하단 전폭.
- **홈·갤러리**: 위 밀도 표대로 열만 늘어남 — 구조 동일. 칩 행은 한 줄 유지(넘치면 가로 스크롤 + 좌우 화살표, 유튜브와 동일).
- **블로그 본문**: 데스크톱에서도 본문 폭 680px 유지 (읽기 폭은 밀도의 예외) — 남는 우측 공간에 목차(TOC) 고정.

### 페이지별 구성 (모바일 기준, 위→아래)

### / (홈 — 템플릿 스토어의 정문)

홈의 일은 하나다: **많은 템플릿 중 자기에게 필요한 것을 찾아 사본 만들기까지.**

1. 헤더: 슉슉이 마크+조현정의AI실험실 로고 (좌) · 검색 아이콘 (우)
2. 업무 태그 칩 행 (가로 스크롤, sticky) — 4개 사용자층의 진입점
3. **템플릿 카드 그리드 첫 묶음** (갤러리형 §3-2, 모바일 2열, 인기순 6개) — 매대가 곧 홈이다. 히어로 없음
4. **책 프로모 배너** (§3-2b) — 템플릿이 먼저 보이되 한 스크롤 안에 책을 만난다
5. "최신 영상" 행 (영상 카드 소형 + [영상 전체 보기]) — 영상은 보조 동선
6. 나머지 템플릿 그리드 (무한 스크롤) + [템플릿 전체 보기]
7. 칩 선택 시 그리드가 해당 태그로 필터 (URL은 /tags/{tag}와 공유)

### /videos (영상 갤러리 — 보조)
1. 헤더 (공통)
2. 태그 칩 행 (sticky)
3. 영상 카드 무한 스크롤 — **자료 연결이 있는 영상만 노출** (SPEC §5-7). 카드 📋 뱃지 = 자료 개수

### /videos/{videoId} (영상 상세 — 짧은 링크 도착 지점)

**폴드 계약**: 스크롤 0 화면 안에 ①컨텍스트 라인 + ②프로모 배너 + ③[사본 만들기] 1개가 모두 보인다. 셋 중 하나라도 밀리면 배너 높이를 줄인다 (사본 버튼이 우선).

1. 헤더 (공통)
2. **컨텍스트 라인** (§3-2c, ~56px): 유입 경로별 문구 분기, 썸네일 탭 → 유튜브 새 탭
3. **프로모 배너** (§3-2b, ≤140px): 책 표지 크게 + 카피 + [자세히 보기]
4. **템플릿 카드** (행형 §3-2, 1~3개): 각각 [사본 만들기] — 첫 카드의 버튼이 폴드 안
5. **"다른 자료 둘러보기"**: 같은 태그 템플릿 갤러리형 카드 4개(2×2) + [템플릿 전체 보기] 필 버튼 — 도착 페이지를 막다른 골목이 아니라 스토어 입구로
6. 하단: 라이선스 1줄 + 채널 링크 + about 링크
- 영상 임베드 없음 (임베드 제거 확정 — §0)
- **태그 칩을 표시하지 않는다** — 태그는 매대의 필터이지 상세의 장식이 아니다. "다른 자료 둘러보기"가 같은 태그 기반 추천으로 그 역할을 대신한다
- 템플릿 0개 영상: 4번 자리에 슉슉이 ⑦ + "이 영상은 보는 것만으로 완성!" + 둘러보기 섹션을 위로 당김

### /templates (템플릿 갤러리 — 스토어 매대, 강의안 포함 모든 자료)
1. 헤더 (공통)
2. 칩 행 2단: 유형 칩(전체/시트/문서) + 업무 태그 칩 (sticky)
3. 정렬 드롭다운: 인기순(기본, 클릭 집계) / 최신순 + 결과 카운터 ("템플릿 47개")
4. 갤러리형 카드 그리드 (§4-0 밀도 표: 모바일 2열 → 데스크톱 4~5열), 무한 스크롤

### /templates/{slug} (템플릿 상세 — 상품 상세, 전환이 일어나는 곳)

매대에서 버튼을 뺐으므로 상세가 전환을 책임진다 (thegoodocs 상세 페이지 패턴):
1. 미리보기 이미지 크게 (탭 시 확대) — "어떤 시트인지" 확인
2. 제목 18px + 한 줄 요약 + 유형·태그 칩
3. **[사본 만들기]** (스크롤 0 안)
4. **사용법** (body_md, 16px — 권장 구조: 무엇이 자동화되나 → 사용 단계 1·2·3 → 포함된 것)
5. 메타: 버전·업데이트일 + 소개 영상 카드(있으면) + 깃허브 링크(연회색 필)
6. 같은 태그 템플릿 4개 (관련 상품)
- (2단계 이후) 사본 횟수 표시 검토 — clicks 집계를 사회적 증거로 [미정]

### /blog, /blog/{slug}
- 목록: 슉슉이 ⑤ 소형 헤더 + 글 카드(제목 16px + 요약 2줄 + 날짜)
- 본문: 16px/28px, 최대 폭 680px, 코드 블록 가로 스크롤, 하단에 연결 영상·템플릿 카드

### /tags/{tag}
- 태그명 헤더 + 설명 1줄 → 필터 칩(전체/영상/템플릿/글) → 혼합 무한 스크롤

### /about
- 슉슉이 ① 인사 + 운영자 소개 → 채널 링크 → 라이선스(MIT/CC BY 4.0) → 출처 표기 안내 → "Google과 무관한 개인 사이트" 명시

## 5. 상태 디자인

### 5-1. 빈 상태
- 검색 0건: 슉슉이 ⑦ 96px + "음… '{검색어}'는 못 찾았어요" + 인기 태그 칩 3개 제안
- 템플릿 없는 영상: §4 영상 상세 참조 — 슉슉이 ⑦ + "다른 자료 둘러보기" 섹션을 위로 (대체 제안 필수, 막다른 골목 금지)

### 5-2. 로딩·오류
- 로딩: 스켈레톤만 (정적 서빙이라 첫 페인트는 즉시). 슉슉이 ⑥은 동기화 대기 등 3초 이상 걸리는 드문 경우만
- 오류(5xx·클릭 실패): 슉슉이 ⑧ + "잠깐 삐끗했어요. 새로고침 한 번이면 보통 해결돼요" + [새로고침] 버튼. 기술 용어(500, DB) 노출 금지
- 죽은 짧은 링크(존재하지 않는 번호): 슉슉이 ③ + "이 번호의 영상은 없어요" + 홈·검색 링크

### 5-3. 사본 만들기 → 구글 권한 화면 안내 (이탈 최다 지점)

`requires_auth=true` 템플릿의 [사본 만들기] 탭 시, 이동 전 **바텀시트 1회 선노출**:

```
┌──────────────────────────────┐
│ 슉슉이 ② (전구, 96px)          │
│ 곧 구글 화면이 떠요            │  ← 18px/700
│                              │
│ 1. [사본 만들기] → 내 드라이브에 복사 │
│ 2. 시트에서 자동화 첫 실행 시        │
│    "확인되지 않은 앱" 경고가 떠요    │
│    → 개인 제작 스크립트라 뜨는       │
│      정상 안내예요               │
│ 3. [고급] → [이동]을 누르면 끝      │
│                              │
│ [ 사본 만들러 가기 ]  ← 전폭 필 버튼 │
│ 다시 보지 않기 (체크박스)           │
└──────────────────────────────┘
```

- 문구 원칙: "경고", "위험" 단어 금지. 무엇이 뜨는지 → 왜 뜨는지 → 어떻게 지나가는지 3줄.
- "다시 보지 않기" 체크 시 localStorage, 이후 즉시 이동. 시트 안 [자세한 안내] 링크 → 스크린샷 포함 블로그 가이드.
- requires_auth=false 템플릿은 이 시트 없이 즉시 이동.

## 6. 접근성·성능 기준 (판정 가능)

| 항목 | 기준 | 검증 |
|---|---|---|
| 터치 타겟 | 버튼 높이 40px 통일 + 상하 여백 포함 실효 터치 영역 ≥ 44px, 그 외 인터랙티브 요소 ≥ 44×44px | 수동 점검 체크리스트 |
| 대비 | 버튼 글자 `#0F0F0F` on `#F2F2F2` = 17.4:1 ✓, 링크 `#0F7A3C` on `#FFF` = 5.4:1 ✓, `#3BC46D` on `#0F0F0F` = 8.5:1 ✓, secondary `#606060` on `#FFF` = 5.7:1 ✓ — 신규 색 조합은 추가 전 계산 필수 | 자동 (CI에 대비 검사 스크립트) |
| 본문 크기 | UI 텍스트 유튜브 스케일(14px), 긴 글 본문 16px | 토큰 강제 |
| LCP | 모바일 4G 기준 ≤ 2.5s (영상 상세·홈) | Lighthouse CI |
| 이미지 | 썸네일 lazy(뷰포트 밖) + 첫 카드만 eager, 슉슉이 WebP ≤ 30KB | 빌드 검사 |
| 영상 임베드 | iframe 사전 로드 금지 — 탭 후 로드 (lite embed) | 코드 리뷰 |
| 폰트 | 시스템 폴백 즉시 렌더(swap), Noto Sans KR 서브셋 woff2 | Lighthouse |
| 인앱 브라우저 | 유튜브 앱 내: 302 체인 정상, 새 탭 열기(/copy) 동작, 뒤로가기 스크롤 복원, 테마 감지 | 1단계 완료 판정에 실기기 테스트 포함 |
| 키보드·스크린리더 | 칩 행 좌우 키 이동, 버튼에 의미 있는 레이블("사본 만들기 — {템플릿명}") | 수동 |

## 7. 하지 말 것

1. **다운로드 앞에 아무것도 세우지 않는다** — 팝업, 뉴스레터, 로그인, 카운트다운. requires_auth 안내 시트(§5-3)가 유일한 예외이고 그것도 1회.
2. 큰 히어로 배너, 풀스크린 일러스트, 그라디언트 배경 금지 — 유튜브에 없는 것은 우리에게도 없다. 유일한 배너는 영상 상세의 프로모 배너(§3-2b)이고 높이 140px를 넘지 않는다.
2b. 상시 안내 문구 금지 (무안내 원칙, §0) — "사본 만들기란…" 류의 설명 줄을 화면에 상주시키지 않는다.
3. 슉슉이 도배 금지 — §2-3. 콘텐츠 영역 진입 금지, 화면당 1회.
4. 그린 남발 금지 — **채움 그린 버튼은 없다.** 버튼은 전부 유튜브 연회색 필. 그린은 로고·뱃지·진행 표시·텍스트 링크만.
5. 자동 재생, 스크롤 하이재킹, 패럴랙스, 진입 애니메이션 금지. 모션은 상태 피드백(pressed, 토스트 슬라이드) 150ms 이하만.
6. 유튜브 로고·레드 재현 금지 — "유튜브처럼 편한"이지 "유튜브인 척"이 아니다.
7. 기술 용어를 방문자에게 노출 금지 — "DB", "500", "ISR", "쿼리 실패" 대신 사람 말.
8. 커스텀 아이콘 제작 금지 — Material Symbols에 있는 것만.
9. 푸터에만 존재하는 중요 정보 금지 — 무한 스크롤이라 푸터는 사실상 없다.
10. "무료지만 출처는 남겨주세요"를 구걸 톤으로 반복 금지 — /about과 템플릿 내 고지로 충분.
